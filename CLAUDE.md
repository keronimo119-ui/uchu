# 宇宙アプリ 作業ルール（全AIモデル必読・このフォルダで作業する前に）

## 最初にやること
1. `README.md`（現状の機能一覧・データ・動作確認方法）を読む
2. 新機能を作るときは `DESIGN.md`（20機能の詳細設計図）の該当セクションを読む
3. 作業開始宣言（1〜3行）をしてから着手する

## この宇宙の不変原則（絶対に守る）
- **実在するものしか描かない。** 架空の演出は禁止。「時間の早回し」「視点の自由」はOK。
  架空要素はBHモードのような「ユーザーが明示的にONにする別世界」にのみ隔離できる。
- 癒し・没頭が目的。派手さより「本物の静けさ」。
- UI・カード・コメントはすべて日本語。専門用語は避け、感情に届く言葉で。
- スマホ(Xperia XQ-CC44)で60fps。1コマの追加コストは常に意識する。
- **BHの質量は固定(900)。** 成長させると全呑み暴走か凍結に転ぶ（実験済み）。
  バランス変更時は `__U.advanceSim(5400)` で90分相当の長期テスト必須。

## 開発の進め方
- **監修方式(全PC必須ルール・2026-07-16)**: 実装は安いモデル(Sonnet等)のサブエージェントへ
  「自己完結の指示書」(対象ファイル・変更点の限定列挙+それ以外変更禁止・検証手順・文書更新・
  commit&pushまで)で委任。監修側はgit差分の要点確認と合否判断のみ。詳細は bisuke\CLAUDE.md。
- 1機能=1セッション。複数機能を混ぜない。部分Editのみ（全体上書き禁止）。
- コード内はセクション見出しコメント `═══` で検索して該当箇所を探す（行番号は当てにしない）。
- 新機能には必ず `window.__U` に検証用フックを足す（画面が見えなくても検証できるように）。
- 機能完成ごとに: プレビュー検証 → スマホ実機確認 → `README.md` 更新。

## 動作確認の手順（ハマりどころ含む）
- プレビュー: launch.json の `uchu-app`（port 8790）。three.jsはローカル同梱済み＝完全オフラインで動く（2026-07-16）。
- ⚠️ プレビュータブは非表示だと描画が1〜2fpsに制限される。実時間待ちで検証せず、
  `__U.advanceSim(秒)` / `__U.hist(0〜1)` 等のフックで状態を直接進めて検証する。
- ⚠️ スクリーンショットは撮影まで3〜10秒かかる。時間で変化する画面は「余裕のある状態」に
  ジャンプしてから撮る（歴史モードは終端間際だと一周してしまう）。
- スマホ表示: `adb -s HQ62CQ0DE6 reverse tcp:8790 tcp:8790` →
  `adb shell am start -a android.intent.action.VIEW -d "http://localhost:8790/?v=N"`（Nはキャッシュ回避で毎回+1）。
- ⚠️ このPCは `adb pair` が壊れている。無線が切れていたらUSBを一度挿して「許可」→無線復活。
- 同じ方法で2回失敗したら3回目はやらず、立ち止まって別の方法を提案する。
