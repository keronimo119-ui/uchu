# 宇宙アプリ 完全設計図 — 機能1〜20の実装ブループリント

作成: 2026-07-03（Fable 5）。目的: **どのAIモデルが引き継いでも同品質で実装できる**こと。
実装前に必ず `CLAUDE.md`（作業ルール）と本書の該当機能セクションを読むこと。

---

## 0. コード地図（index.html の構造）

単一ファイル `index.html`（+ 星データ `stars.js`）。セクション見出しコメント `═══` で検索して探す。

| セクション | 内容 | 主要シンボル |
|---|---|---|
| 日本語名 | 星座88・恒星80の和名、星座紹介文 | `JP_CON` `JP_STAR` `CON_INFO` |
| 星の色 | B-V→色温度→RGB（実測ベース） | `bvToRGB()` |
| 基本セットアップ | renderer(autoClear=false)・camera(pc単位) | `PR`(pixelRatio上限2) |
| 星のシェーダー | 全星共通Points材質。等級→輝度/サイズ、回折スパイク | `starMat` `starUniforms.uSize/uTime` |
| 実在の星+天の川 | 8,920実在星+太陽(idx=`nSun`)+プロシージャル微光星 | `pos/col/mag/pha` `absMag` `updateApparentMags()` |
| 乳白色のもや | 天の川の集合光（大点スプライト） | `fogMat` `fogUniforms` |
| 銀河・星雲 | M31/マゼラン雲/M42/すばる 実位置スプライト | `glowSprite()` `radec()` |
| 星座線 | 全88 + 選択ハイライト + 検索用データ | `consLines` `hlLines` `conSegs` `conDir` `conSeason()` `conBrightest()` |
| 新生星（BHモード） | 銀河形成シミュ | `nb[]` `spawnStar()` `doomOldest()` `updateNewborn()` `writeNbBuffers()` `bhMass`(固定900) `GAL_AXIS` |
| ブラックホール | ポストプロセス（重力レンズ/光子リング/降着円盤） | `postUni.uBH/uRpx/uOn/uFlash` `RS_WORLD` |
| 歴史モード | 地球46億年。別シーン(単位=地球半径) | `hScene` `hCam` `earthUni` `ERAS[17]` `updateHistory()` `histP` |
| カメラ | 3モード: `mode`='orbit'/'gaze'/'history' | 下表参照 |
| タップ→カード | 星ピッキング+情報カード | `pickStar()` `card` |
| UI | ボタン7個・星座パネル・現在地HUD | `wakeUI()` `selectCon()` `panelEl` |
| 環境音 | 生成式アンビエント | `startSound()` `snd` |
| メインループ | 時間進行→カメラ→描画合成 | `loop()` `advanceSim()` `SIM.speed` |

### カメラ3モードの契約
| mode | 位置 | 注視 | 操作変数 | 入退場 |
|---|---|---|---|---|
| orbit(宇宙遊泳) | 太陽中心の楕円軌道 r=2.5..27pc | 常に原点 | `camTh/camPh/vTh/vPh/rPhase` | 既定 |
| gaze(地球の夜空) | 原点(camR→0.02) | 自由視線 | `gazeLon/gazeLat/vgLon/vgLat` | `enterGaze()/exitGaze()` |
| history(46億年) | 地球周回(単位=地球半径) | 常に地球 | `hTh/hPh/hR/histP` | `bHist`ハンドラ/`exitHistory()` |

新モードを足すときはこの表に倣う: 状態変数一式+enter/exit関数+pointermove分岐+loop分岐+HUD分岐+`pickStar`ガード。

### 座標系・単位（不変）
- 赤道座標J2000。**+Z=天の北極**、距離単位=パーセク(pc)。光年= pc×3.2616。
- RA/Dec→XYZ: `radec(raDeg, decDeg, dist)` を使う。
- 銀河座標→赤道座標: `galToEq()`（天の川生成で使用済み）。
- 星の明るさは絶対等級`absMag[]`から**カメラ実距離で毎コマ再計算**（`updateApparentMags`）。
  見かけ等級を直接いじる機能（変光星等）はこの関数の中に差し込むこと。

### 検証フック `window.__U`（新機能ごとに必ず追加する）
`bench(n)` 描画ms実測 / `dbg()` 状態一覧 / `dist()` 新生星の距離分布 / `advanceSim(秒)` 時間一括進行 /
`hist(0..1)` 歴史ジャンプ / `testPick(名前)` `pickCenter()` タップ再現 / `setCam(th,ph)` / `sim.speed` / `getMode()`

---

## 1. データ基盤

### stars.js（自動生成・手編集禁止）
`window.UCHU_DATA = { cons:[88略号], stars:[[x,y,z,mag,ci,conIdx]...], names:{idx:"Sirius"}, lines:[idx,idx,...] }`
- 生成元: HYG v4.1 `https://raw.githubusercontent.com/astronexus/HYG-Database/main/hyg/CURRENT/hygdata_v41.csv`（33MB）
  フィルタ mag≤6.5 → 8,920個。距離不明(dist>9000)は半径2000pcに正規化（=「距離不明」の目印）。
- 星座線: Stellarium `https://raw.githubusercontent.com/Stellarium/stellarium/master/skycultures/modern/index.json`
  （`constellations[].lines` がHIP番号のチェーン。88星座692本）
- 再生成スクリプトの書き方はREADME「stars.jsの再生成方法」参照（scratchpadのprocess.jsは消えている可能性あり。
  同じ要領で書き直せる: CSVは引用符対応パーサ必須、HIP→星indexマップで線を解決）。
- **列の追加**（機能1・4で必要）は process.js に列を足して stars.js を作り直す。既存フォーマットは末尾追加のみ
  （`[x,y,z,mag,ci,conIdx, 新規...]`）で後方互換を守る。

### 実装済みの実データ根拠（触るときに壊さない）
- 色: B-V→色温度(Ballesteros式)→RGB(黒体近似)。
- 月の距離: 誕生時17地球半径→現在60（実史実）。近い月ほど速く公転（ケプラー）。
- 星座の見頃: 中心方向のRAから `月 = 3.7 + ((RA時 - 9 + 24) % 24) / 2`（21時頃に南中する月）。

---

## 2. 機能1〜20 詳細設計

規模: S=1〜2時間 / M=半日 / L=1日。各機能とも最後に「READMEに1行追記+`?v=N`でスマホ確認」まで含む。

### 1. 固有運動タイムラプス（星座が本当に崩れる）【L・目玉】
- **根拠**: HYGに全星の実測速度がある。±10万年程度なら直線外挿が科学的に妥当。
- **データ**: process.js改修。HYGの `vx,vy,vz` 列（単位はpc/年。**要検証**: シリウスで
  |v|×97.8万 ≒ 秒速km換算で数十km/sになるはず。おかしければ単位を疑う）。
  stars.jsの各星に `[..., vx*1e6, vy*1e6, vz*1e6]`（=pc/百万年, 小数4桁）を追加。+約200KB。
  距離不明星(r=2000)は速度0にする。
- **実装**: 新モードではなく**時間スライダー**（orbit/gazeで有効）。`timeYr`（-100000..+100000年）。
  `updateApparentMags` の直前で `pos'=pos+v*timeYr` を別バッファ`posT`に書き、position属性を差し替え
  （毎コマではなくtimeYr変更時のみ再計算）。星座線`linePos`とハイライトも同時に再計算。
  ピッキングと最寄り星計算は`posT`参照に変更。UI: ⏱ボタン→下部にスライダー+「西暦◯◯年/◯万年前」表示、
  離すと現在に戻るボタン。
- **検証**: `__U`に`setTime(yr)`追加。北斗七星(おおぐま座)を+5万年でスクショ→柄が曲がる。
  アークトゥルス(高速星)の移動量が大きいこと。0年で完全に現状と一致すること。
- **落とし穴**: 8920星×再計算はtimeYr変更時のみ（スライダー中は間引き）。絶対等級は距離変化で
  自動追従（updateApparentMagsがposT基準になるため）。

### 2. 誕生日の星【M・感動枠】
- **根拠**: N歳の人には「N光年先の星の、生まれた年に出た光」が今届いている。全部実距離。
- **データ**: 不要（既存の距離から計算）。
- **実装**: パネルに「🎂あなたの星」ボタン→西暦年入力(input type=number)→年齢=現在-生年→
  `|r×3.2616 - 年齢| < 0.7` の星から最も明るい既知距離星を選ぶ→gazeモードでseek→専用カード
  「あなたが生まれた年、この星を光が発ちました。◯◯年かけて、いまあなたに届いています」。
  localStorageに生年保存、次回から1タップ。候補が複数あれば「次の星」ボタンで巡回。
- **検証**: `__U.birthday(1990)`フック→カード内容と星の距離が年齢±0.7lyか確認。
- **落とし穴**: 0〜4歳は該当星が少ない（4.4ly α Cenが最近接）→「光はまだ旅の途中」表示に切替。

### 3. 光害シミュレータ（都会⇄砂漠の空）【S】
- **根拠**: 限界等級は実概念（都会≈3等/郊外≈4.5/山≈5.8/砂漠≈6.5）。
- **実装**: starMatに `uMagLimit`（既定99）。vertexで
  `vInt *= smoothstep(uMagLimit + 0.8, uMagLimit - 0.2, aMag + 5.0*(1.0-step(縮尺...)))`…実装は単純に
  `vInt *= smoothstep(uMagLimit+0.8, uMagLimit-0.2, aMag)`。fog(天の川のもや)は都会で opacity 0。
  post shaderに `uSkyGlow`（都会の空は真っ黒でなく灰青: `c += vec3(0.02,0.03,0.05)*uSkyGlow`）。
  UI: gazeモード時のみ有効なボタン「空」→ 都会/郊外/山/砂漠/宇宙 を巡回、右下HUDに現在値。
- **検証**: 各プリセットでスクショ、星数が段階的に減ること・都会で天の川消失。

### 4. 系外惑星をもつ星【M】
- **根拠**: NASA Exoplanet Archiveの確定惑星。
- **データ**: TAP: `https://exoplanetarchive.ipac.caltech.edu/TAP/sync?query=select+hostname,hip_name,hd_name,sy_dist,disc_year,pl_name,pl_orbper,pl_bmasse+from+ps+where+default_flag=1&format=csv`
  （列名は実行時に要確認）。HYGの`hip`/`hd`列と突合 → 見える星(8920)のうち惑星ホスト（数百個）に
  フラグ+惑星数+代表惑星名を stars.js 新配列 `planets:{starIdx:[惑星数, "51 Peg b", 発見年]}` で追加。
- **実装**: カードに「🪐 惑星が◯個見つかっている星（1995年発見の51 Peg bなど）」行を追加。
  ✦ボタン長押し or パネルに「惑星のある星を光らせる」トグル→該当星の色を一瞬リング表示
  （リングは点滅ハイライト用の小さなPointsレイヤー、常時表示はしない=リアル優先）。
- **検証**: `testPick('Fomalhaut')`等、著名ホスト星のカードに惑星行が出る。
- **落とし穴**: psテーブルは1惑星=複数行のことがある→hostnameでdistinct集計。

### 5. 変光星の実挙動【M】
- **根拠**: 実周期・実光度幅。現在時刻から実位相を計算すれば「今夜のアルゴルの明るさ」になる。
- **データ**(本書に同梱、実装時にepochだけWeb確認推奨):
  `Algol P=2.8673日 2.12→3.39等(食)` / `Mira P≈332日 2.0→10.1等` / `δ Cep P=5.3663日 3.5→4.4` /
  `β Lyr P=12.94日 3.3→4.4` / `η Aql P=7.177日 3.5→4.4` / `μ Cep(ガーネットスター) 半規則` /
  `R Lyr` `χ Cyg P≈408日` など10〜20個。名前→星idxは `D.names` から解決。
- **実装**: `VARIABLES=[{idx,P,m0,m1,type,epochJD}]`。`updateApparentMags`内で該当星だけ
  `absMag+Δm(位相)` を加算（食型は谷が鋭い形、脈動型はsin近似）。位相=実時刻JDから。
  カードに「変光星: いまは◯等（◯日周期で2.1〜3.4等）」。タイムラプス(機能1)のスライダーと連動可。
- **検証**: `__U.varPhase('Algol', 0.5)`強制フックで極小時に暗くなるスクショ。
- **落とし穴**: Miraは極小10等=見えなくなる（それが正しい）。

### 6. 星の一生（この星の未来）【M】
- **根拠**: 色(B-V)と絶対等級からHR図上の位置→質量→寿命・末路は天文学の標準推定。
- **実装**: データ追加不要。`stellarFate(ci, absMag)`関数: 主系列判定→質量近似
  （M☉≈10^((4.83-absMag)/10)程度の粗い式でよい、注記つき）→分類:
  赤色矮星「ゆっくり数兆年輝き続ける」/太陽型「50億年後に赤色巨星→白色矮星」/
  大質量「数百万年で超新星→中性子星/BH」/巨星「すでに晩年」。
  カードに「この星の物語」折りたたみ行(タップで展開)として追加。
- **検証**: ベテルギウス→超新星予告、シリウス→白色矮星連星の言及、太陽→50億年。
- **落とし穴**: 断定調を避け「〜と考えられています」。巨星/主系列の判別は absMag<0 かつ ci>1 なら巨星程度の粗さで可。

### 7. ベテルギウス超新星シミュレーション【M・遊び】
- **根拠**: 実予測（爆発したら最大約-12等=半月級、数週間輝き数ヶ月で減光）。
- **実装**: ベテルギウスのカードに「💥もし今夜爆発したら(シミュレーション)」ボタン→
  gazeでベテルギウスへseek→60秒のタイムラプス（字幕: 爆発当日→1週間→3ヶ月→2年）で
  `absMag`を実光度曲線どおり変化(+ハロー強調のためその星のみsize上限解放)→終了後に元へ戻し
  「実際にいつ起きてもおかしくないと言われています(明日かも、10万年後かも)」カード。
  明示的に「シミュレーション」ラベルを常時表示（リアル原則）。
- **検証**: `__U.betelgeuse()`で発火、ピーク時スクショ(圧倒的輝点+スパイク)。

### 8. 流星群カレンダー【M】
- **根拠**: 主要流星群の放射点・極大日・出現数(ZHR)は毎年ほぼ固定。
- **データ**(同梱): しぶんぎ1/4(RA230,Dec49,ZHR110) / こと座4/22(271,34,18) / みずがめη5/6(338,-1,50) /
  ペルセウス8/13(46,58,100) / オリオン10/21(95,16,20) / しし11/17(152,22,15) / ふたご12/14(112,33,150)。
- **実装**: 起動時に日付判定、極大±4日なら有効。gazeモード中、放射点方向から実頻度スケール
  （ZHR/60×時角補正は簡略可）で流星: 短い線分ジオメトリを放射点から外向きに0.3〜0.7秒で
  伸ばして消す(additive・白〜緑)。HUDに「今夜はペルセウス座流星群(極大)」。
  日付外は完全に出さない（リアル原則）。`__U.meteor(強制)`フック。
- **検証**: 日付偽装フック→流星の向きが放射点から放射状であること。

### 9. ボイジャー1号（人類最遠の証）【S】
- **根拠**: 実位置。2026年時点で太陽から約168AU、へびつかい座方向(RA≈258°,Dec≈+12°)、毎年+3.6AU。
  実装時に https://voyager.jpl.nasa.gov/mission/status/ で現在値を確認して定数更新。
- **実装**: orbitモードで太陽近傍を見たとき、金色の極小マーカー(Points 1個・タップ可能半径大きめ)。
  タップ→「ボイジャー1号 ─ 1977年に地球を発ち、いま太陽から◯◯AU。光の速さでも約23時間かかる場所。
  あなたの見ている星々には、まだ遠く及ばない」= 宇宙の広さの実感装置。V2/ニューホライズンズも同様に可。
- **検証**: `pickVoyager()`フック→カード表示。距離が日付連動で増えること。

### 10. 星の音色化【S】
- **根拠**: 色温度→音高のマッピング(共感覚的だが「データ駆動」)。パルサーのリズムは実周期。
- **実装**: 音ON時、星タップで `startSound`のctxに短い音: 周波数=map(色温度3000..30000K → 130..1050Hz)、
  音量=map(等級)、既存のディレイへ送る。カードと同時に「この星の音」が鳴る。
  かに星雲パルサー(30.2Hz)・PSR B0329+54(0.714秒)は専用マーカー+実リズムのクリック音(数秒)。
- **検証**: タップでオシレータ生成をフックで確認、ベテルギウス(低温)が低い音、ベガ(高温)が高い音。

### 11. 距離はしご体感モード【L】
- **根拠**: 実スケール比。「宇宙の遠さ」を体で感じる旅。
- **実装**: パネルに「🚀宇宙の果てへ」→スクリプト再生(約2分、タップでスキップ/中断):
  歴史モードの地球(1地球半径)から出発→月(60半径)→太陽(23,500半径)→[シーン遷移]→
  orbitモードでボイジャー→α Cen(1.3pc)→すばる(136pc)→天の川の帯(数千pc)→M31(78万pc)。
  各停留所で字幕「月まで: 光で1.3秒」「太陽: 8分19秒」「α Cen: 4.4年」「M31: 250万年」。
  カメラはease-in-outで対数的にズームアウト。既存の2シーン(hScene/main)を跨ぐ演出は
  「太陽で一度フェード→orbitモードへ」で簡潔に。
- **検証**: `__U.journey(stage)`で各停留所ジャンプ+スクショ5枚。
- **落とし穴**: スケール差が巨大なのでカメラ半径は対数補間。字幕の光時間は実値を使う。

### 12. 古典星図オーバーレイ【M・要アセット判断】
- **根拠**: Stellarium modernの星座絵(パブリックドメイン系)。`index.json`の`image`フィールドに
  PNGファイル名と**アンカー3点(画像px↔HIP星)**が入っている＝3点アフィンで天球に貼れる。
- **実装**: 絵は容量が重い(88枚)→まず黄道12+オリオン等15枚に限定、512px WebP化(計~1MB)。
  `https://raw.githubusercontent.com/Stellarium/stellarium/master/skycultures/modern/illustrations/◯.png`
  をDL→変換→`art/`フォルダ。選択星座のみ表示(常時表示しない)。3点アンカーからPlaneをワープ
  (アンカー星3つの3D位置にUV3点を対応させたBufferGeometry三角形2枚+テクスチャ)。opacity 0.25。
  `selectCon()`に組み込み「絵」トグル。
- **検証**: オリオンの絵が三ツ星・リゲル・ベテルギウスに合うこと(アンカーが合えば自動で合う)。
- **落とし穴**: ライセンス表記をREADMEへ(Stellariumのart作者クレジット)。オフライン同梱前提の容量管理。

### 13. 惑星と「今夜の見どころ」【L・実装価値大】
- **根拠**: 惑星は肉眼夜空の主役なのに現状不在。ケプラー軌道要素の近似計算で±1°精度は容易。
- **データ**: JPL近似軌道要素(1800-2050) https://ssd.jpl.nasa.gov/planets/approx_pos.html の表を
  コードに定数として転記（8惑星+月は別式）。月位相は簡易式(朔望月29.53日、基準新月JD2451550.1)。
- **実装**: `planetPositions(date)`→各惑星の黄道座標→赤道座標→gazeモードの星空に実等級・実色で
  Points追加(金星-4等の圧倒的輝き、火星の赤)。タップカード「木星 ─ いま おとめ座に。-2.3等」。
  タイムラプス(機能1)と連動すれば惑星がするする動く=「惑星(さまよう星)」の語源体験。
  起動時カード「今夜の見どころ」: 月齢・見えている惑星・(機能8の)流星群を1枚に。
- **検証**: 実装日の実際の惑星位置とWeb照合(±2°)。`__U.planets()`でRA/Dec一覧出力。
- **落とし穴**: 月の位置は精度が出にくい(±2°でOKとする)。orbitモード(太陽系外視点)では
  惑星は太陽の点に埋もれる=非表示でよい(リアル)。

### 14. 撮影モード（日周運動の星の軌跡）【M】
- **根拠**: 長時間露光写真=実技法。北極星を中心に星が弧を描くのは地球の自転。
- **実装**: gazeモード専用「📷軌跡」トグル→postの最終段で前フレームを0.985倍で混ぜる
  蓄積バッファ(ping-pong RT 2枚)+視線を天の自転(15°/時×早回し1000倍)で回す→星が弧を描く。
  もう一度押すと通常へ。「保存」ボタン: `renderer.domElement.toDataURL('image/png')`→aタグdownload。
- **検証**: 60秒後スクショ=北極星付近を中心とした同心円弧。保存PNGが開けること。
- **落とし穴**: preserveDrawingBuffer:false のため保存はrender直後に同期取得(または一時RTから読む)。

### 15. ジャイロ連動（かざして星座）【M・実機のみ】
- **根拠**: プラネタリウムアプリの定番=本物の空との照合。
- **実装**: gazeモードに「📱かざす」トグル→`DeviceOrientationEvent`(alpha/beta/gamma)→
  端末の向き(方位+高度)→観測地(Geolocation、拒否時は東京)+現在時刻から地方恒星時LST=
  `280.46+360.9856474×(JD-2451545)+経度`→方位/高度→時角/赤緯→gazeLon/gazeLatへ。
  センサーは平滑化(lerp 0.1)。
- **検証**: 実機必須。北にかざして北極星が出る/スマホを上に向けると天頂の星座。
  PCでは`__U.gyro(az,alt)`偽装フックで数学だけ先に検証(例: 東京で北・高度35°→ポラリス)。
- **落とし穴**: WebView化の際にpermission設定が要る(Android manifest)。方位はコンパス精度依存。

### 16. しおり（視点の保存）【S】
- **実装**: パネルに「🔖しおり」タブ: 現在のmode/視線/星座選択/名前(プロンプト入力)を
  localStorage `uchu_marks[]` に保存。一覧タップで復元(seek)。削除スワイプ or 長押し。
- **検証**: 保存→リロード→復元eval。

### 17. 未来の空（歴史モードの延長）【M・おすすめ】
- **根拠**: 全部実予測: 5億年後=太陽光度上昇で地球は温室化/10〜40億年後=アンドロメダ銀河が
  接近し夜空の半分を覆って衝突(ミルコメダ)/50億年後=太陽が赤色巨星化。
- **実装**: `ERAS`の後ろに未来4時代を追加: 「5億年後 ─ 灼熱化する地球」(雲多・茶色化)、
  「40億年後 ─ アンドロメダ衝突」(背景にM31スプライトを巨大化表示+空が星で埋まる演出は
  実予測の範囲で控えめに)、「50億年後 ─ 太陽、赤色巨星に」(太陽グロー巨大化・地球は焦土色)、
  「その後 ─ 白色矮星の静かな光」。ループは「現在」で3秒停止→未来へ、を明示する字幕
  「ここから先は未来(実予測)」。
- **検証**: `hist()`ジャンプで各未来時代スクショ。
- **落とし穴**: 歴史ループ終端のスクショはタイミングに注意(CLAUDE.md参照)。

### 18. 隠れブラックホール探し【M・ゲーム】
- **根拠**: 本物のBH探索も「背景の星の歪み(重力レンズ)」で見つける。同じ原理の遊び。
- **実装**: BHボタン長押し(またはパネル)→「探索モード」: postUniに`uHideBH`を追加し
  リング/円盤/フラッシュ項をスキップ(レンズの曲がりだけ残す)。BHは通常どおり空を移動。
  プレイヤーが歪みを見つけてタップ→BH画面位置と±4°以内なら「発見!」(リング表示+所要時間)。
  見つからないとき用に30秒ごとに小さなヒント(「いて座の方向が怪しい…」=BH方向の星座名)。
- **検証**: `uHideBH=1`でリング非表示+歪みだけのスクショ/タップ判定フック。
- **落とし穴**: 歪みが分かるのは背景に星が多い領域だけ=それがゲーム性(本物と同じ)。

### 19. 元素の物語【S】
- **根拠**: 元素の起源は恒星内部・超新星・中性子星合体(標準理論)。
- **実装**: カードの「この星の物語」(機能6)に続けて元素行: 星種別ごとに定型文
  「この重さの星は炭素・酸素を作って宇宙に返します。あなたの体の炭素も、こうした星の贈り物」
  「この星はやがて超新星となり、鉄や金を宇宙にまき散らします。地球の鉄はその子孫」。
  文面テーブル`ELEMENT_STORIES`を6分類ぶん用意。
- **検証**: 数種の星で文面切替を確認。

### 20. 二重星めぐり【M】
- **根拠**: 実在の色対比の美しい二重星。望遠鏡観察の定番コース。
- **データ**(同梱・実装時に離角を再確認): アルビレオ(金3.1等+青5.1等、離角35″)/ミザール&アルコル(12′)/
  γ And(金+青、9.6″)/ε Lyr(ダブルダブル)/β Cyg=アルビレオ/α CVn/γ Leo/ι Cnc/β Sco/α Her。
- **実装**: パネル「💎二重星ツアー」→順にseek+ズーム(fov最小を8°まで拡張=双眼鏡相当と表記)。
  離角が分単位のもの(ミザール等)はデータに両星が別々に入っており実際に2点に分離して見える。
  秒単位のもの(アルビレオ等)は1点のまま+カードで「望遠鏡では金と青の宝石」(2色の色見本チップを
  カード内に表示=画面上は実視に忠実、知識はカードで)。
- **検証**: ミザール&アルコルがズームで2点に分離するスクショ/アルビレオのカード色チップ。

---

## 3. 実装順序の推奨

**難しい順にFable在任中(残り4日)に片付けるのが得**:
- Day1: **13 惑星+今夜の見どころ**(L・数学が重い) → **1 固有運動タイムラプス**(L・データ再生成含む)
- Day2: **11 距離はしご**(L・演出調整) → **17 未来の空**(M)
- Day3: **8 流星群** + **5 変光星** + **7 ベテルギウス**(M×3・天文計算系をまとめて)
- Day4: **14 撮影モード** + **18 隠れBH**(描画系) + 仕上げ検証
- Sonnet以降でも安全(設計が単純): 2誕生日 / 3光害 / 6+19物語 / 9ボイジャー / 10音 / 16しおり / 20二重星 / 4系外惑星 / 12星図絵 / 15ジャイロ(実機検証だけ丁寧に)

## 4. その先（機能20の後）
1. **オフライン化**: three.module.js(約1.2MB)をローカル保存しimportを`./three.module.js`へ。
   星図絵・音は既に自前。フォントはシステムフォントのまま。
2. **Android化**: mochi方式(WebView)。`Desktop\AI\mochi-project`の構成を参照。フルスクリーン・
   スリープ抑止(WakeLock)・ジャイロ/位置のpermission。
3. **AdMob**: バナーは全画面モードでは非表示(没頭優先)、通常時のみ下部。手値のAdMob導入手順を流用。
4. パフォーマンス予算: 追加レイヤーは常に `__U.bench(60)` で1コマ2ms以内を維持。
