# 宇宙アプリ 引継ぎノート（HANDOFF）

最終更新: 2026-07-11（Opus 4.8）／ 次回は別モデルの可能性が高い。
**読み順: ①`CLAUDE.md`（作業ルール・必読）→ ②`README.md`（現状機能）→ ③`DESIGN.md`（次に作る20機能の設計図）**

## 現在の状況（すべて動作確認済み・やりかけ作業なし）
- 単一HTML（index.html + stars.js）。実在の星8,920個（HYG v4.1）・色/明るさ/位置すべて実データ。
- 3つのカメラモード: 宇宙遊泳（下記フリックの法則・2026-07-07全面作り直し）／🌍地球の夜空／⏳歴史（46億年・17時代）。
- **遊泳の「フリックの法則」（ユーザー承認済み・変更時は必ず確認を取ること）**:
  指を離した瞬間に1ダイブ＝①フリック方向へ少しホップした奥へ超スピード直進(vMax65)
  ②勢いが尽きたら(壁r=42で強制減速含む)自分を中心にフリック方向へゆっくり回頭(0.35rad/s・約8秒)
  ③中心をとらえたら終了→中心を見るランダム浮遊へ。回頭中の再フリック=即再ダイブ。詳細はREADME。
- 歴史モード: ピンチで地球・月も星と連動ズーム(hCam.fov同期)。惑星5個(水星〜土星)を実在色の点光で表示。
- **🪐太陽系めぐり(2026-07-08〜11完成・検収合格済み)**: 太陽/地球/月/火星/木星/土星を左右フリックで
  めぐる新モード(端はループ)。実自転軸/周期3600倍速・土星の環は実比率+間隙。放置中は漂う浮遊カメラ。
  飛行中は昼側構図へ自動復帰。到着時に日本語カード(カードは触れた瞬間に閉じる)。
  **地球・月はNASA由来の実写マップ(tex/にローカル同梱)**+雲/海の照り返し/大気/夕焼け/街あかり(2026-07-11)。
  太陽は赤く燃える配色。**縦フリックで軌道図ビュー**: 太陽中心に軌道の輪、実周期比(1地球年=120秒)で公転、
  月は地球のまわり。星タップ=解説/もう一度=接近/空タップ=閉じる。
  横画面対応済み(tCam.aspect+低い画面はボタン縮小)。詳細はREADME・設計書はdocs/plans/。
- **惑星と今夜の見どころ(2026-07-25完成・DESIGN機能13)**: JPL近似軌道要素(1800-2050)のケプラー計算で
  水星〜土星の**本物の位置**を求め、🌍地球の夜空(gaze)モードだけに実等級・実色の点で表示(星と同じ
  `starMat` 共用・方向×100pcをカメラ位置に追従)。タップで「木星 ─ いま かに座に／-1.6等 ／ 地球から
  9.42億km／見え方／一言」。起動1.5秒後に「今夜の見どころ」カード(月齢＋見えている惑星)。
  orbit/history/tourでは非表示(太陽の光に埋もれるのが本物)。星座は**黄道上の境界での近似判定**。
  検証: `__U.planets()` `__U.tonight()` `__U.setPlanetDate(ISO)` `__U.planetPick(名)` `__U.planetTick()`。
  太陽黄経の検算=2026-07-25T12Z→122.19°、2000-01-01T12Z→280.38°。設計書はdocs/plans/2026-07-25-。
  **実機(XQ-CC44)確認済み**: 金星に視線を合わせてタップ→「金星 ─ いま しし座に／-4.1等 ／
  地球から 1.27億km／夕方の西の空に見えます」。実際の2026年7月下旬の空(金星=しし座・木星=合)と一致。
- **固有運動タイムラプス(2026-07-25完成・DESIGN機能1)**: ⏱時間ボタン→下部スライダーで±10万年。
  8,920星がHYGの**実測速度**で動く(直線外挿)。データは `starvel.js`(自動生成170KB・pc/百万年)、
  生成は `tools/gen-starvel.js`(HYG CSVを落として既存starsと方向+等級で照合。**stars.jsは再生成しない**
  =星座線がindex参照のため)。**距離不明星と600km/s超は速度0**(架空の動きを持ち込まないため)。
  星座線・ハイライト・星タップ・最寄り星も同じ時刻の座標を使う。宇宙遊泳と地球の夜空でのみ表示。
  検証: `__U.setTime(年)` `__U.timeState()`。実測: 北斗七星の柄の角度(アリオト-ミザール-アルカイド)は
  いま141.6°→10万年後77.4°→10万年前167.5°。アークトゥルスは5万年で29.5°移動(固有運動2.28″/年と一致)。
- **星タップのハイライト演出(2026-07-26・ユーザー要望「どれを選んだか分からない」)**: タップした星が
  **一瞬だけ明るくなる**(0.8秒)。実在星・太陽・惑星の3経路とも同じ `pickFlash(位置, 色, 追従, 等級)` を呼ぶ。
  1点のPoints+専用シェーダーだが、**大きさ・明るさは星のシェーダーと同じ法則**(`clamp(10-m,1.6,22)`,
  `pow(10,-0.16m)*2.6`)で、選んだ星の見かけの等級を2.5等ぶん一瞬上げた姿を描く=暗い星は小さく、
  明るい星は大きく輝く。**ユーザー却下の設計(やり直し2回)**: ①光の輪・二重丸=人工物でリアルさが壊れる
  ②一律サイズの光=「空間が光る」に見えて星を選んだ感じにならない。演出は必ず星の描画法則に従わせること。
  検証: `__U.flashState()` `__U.flashTick(dt)`。**演出の見た目を実機で撮るコツ**: 描画ループが毎コマ uTime を
  上書きするので、`Object.defineProperty(mat.uniforms,'uTime',{get:()=>({value:0.45})})` で凍結してから撮る。
- UI: モードボタン等は3秒で自動消去(タップ復帰)。左端に縦の「遊泳速度」スライダー(漂い0.4〜4倍・遊泳中のみ)。
- 🔍星座図鑑88件、星タップカード、太陽タップ、現在地HUD、●BH隠しモード。
- 音(2026-07-16夜・第2版): 和音進行(Am9→F→C→G6)+操作連動SFX(`SFX.*`: whoosh/chime/arrive/boom)+
  暴走中は生成ロック(rockOn/rockOff)。ボタンなし・最初のタッチで常時ON。検証は `__U.sndState()`/`__U.sfx`。
- 🚀暴走モードは実装→ユーザー判断で**廃止**(2026-07-16。「空間が狭く感じる」ため。エリアr=42では
  高速移動が回転にしか見えないという学び)。生成ロック(SFX.rockOn/rockOff)は休眠コードとして残置=
  将来使う時はどこかから呼ぶだけ。A案実装済み(rMax260+距離連動速度・2026-07-16)。
- 軌道図ビュー(2026-07-16): 輪は加算発光0.8、漂いは速度バー連動、ピンチ=太陽系へ接近(`tourZoom`)。
- スマホ実機（Xperia XQ-CC44, serial HQ62CQ0DE6）でフリック法則まで動作確認済み。

## 次にやること
1. **太陽系めぐり第2弾**: 水星・金星・天王星・海王星を追加（駅を足すだけの作りにしてある。
   tourStation()に4駅追加+シェーダー4つ+TOUR_INFO4枚。金星は自転が逆向き=spinを負に）。
2. **ユーザーの不満あり:「スマホで閉じると後で開けない」**→ オフライン化は2026-07-16完了
   (three.module.jsローカル同梱・外部依存ゼロを実機確認済み)。残る恒久対策=Androidアプリ化(mochiと同じ方式)。
   **広告の要望(2026-07-11・ユーザー構想)**: バナーは画面最上部・70秒表示したら自動で消え、
   次のバナー広告カウント発生条件まで出さない、という動きにしたい。AdMob組込み時に
   ポリシー(非表示中のリフレッシュ禁止等)を確認しながら設計すること。
3. ~~機能13「惑星と今夜の見どころ」~~ ~~機能1(固有運動タイムラプス)~~ → **どちらも2026-07-25完成**(上記)。
   次は DESIGN.md から選ぶ。候補: 機能2「誕生日の星」(M・感動枠)／機能8「流星群」／太陽系めぐり第2弾(上の1)。
   注意: `PLANETS`/`planetSprites`/`updatePlanets`(742行付近)は歴史モード専用の雰囲気表現のまま残置。
   本物の惑星計算は別セクション(`helioVec`/`planetPositions`)。混同しないこと。
   なお時間スライダーは惑星には効かない(惑星は現在時刻の位置)。連動させるなら次の課題。
4. **「星の物語」導入(ユーザーがNotionで執筆中・閲覧許可済み)**: 場所=Notionページ
   「宇宙アプリ コンテンツ設計」(id: 3a0644bd-ed76-8174-812d-f4009945de86)配下。
   構成=星座ごとに1ページ(タイトル形式「さそり座 — 逃げ続ける狩人と、追い続ける毒針」)+
   **文化別バリエーション**(「さそり座・日本」「さそり座・中国」等。和名・逸話つき)。
   導入時: notion-search/fetchで取得→星座図鑑カード(selectCon)の拡張として「物語を読む」展開+
   文化切替を想定。データはJSON別ファイル(stories.js等)に書き出して同梱(オフライン原則)。
   執筆途中の可能性があるため、導入前にユーザーへ「どこまで書けたか・どの星座から入れるか」を確認。
5. **実装は監修方式で**(2026-07-16全PC必須化・bisuke\CLAUDE.md正本): 安いモデルのサブエージェントに
   自己完結指示書で委任し、監修はgit差分確認と合否判断のみ。本HANDOFFの読み手が安いモデル単独の場合は
   そのまま実装してよい(/kensaで検収)。
- 1機能=1セッション。完成ごとに README更新+`?v=N`をインクリメントしてスマホ確認。
- `index.html.bak` はコミット対象外の作業バックアップ(不要になったら削除可)。

## 環境・手順（このPC=s-sigpc）
- プレビュー: `.claude/launch.json` の `uchu-app`（port 8790、`serve.ps1`はlocalhost専用）。
- スマホ表示: `adb -s HQ62CQ0DE6 reverse tcp:8790 tcp:8790` → `adb shell am start -a android.intent.action.VIEW -d "http://localhost:8790/?v=N"`
- 検証はコンソールの `window.__U` フック経由（CLAUDE.md参照）。
- **スマホでJSを実行する方法(2026-07-25確立・デスクトップのrAF停止を回避できる決定打)**:
  `adb forward tcp:9222 localabstract:chrome_devtools_remote` → `http://localhost:9222/json` の
  `webSocketDebuggerUrl` へ PowerShellの `System.Net.WebSockets.ClientWebSocket` で接続し
  `{"id":1,"method":"Runtime.evaluate","params":{"expression":"...","returnByValue":true}}` を送る。
  実機の `__U.*` が全部叩ける＝実機で決定論的に検証できる（今回これで金星のカードを確認した）。
- **無線adbのポートはスマホの画面表示と食い違うことがある** → `adb mdns services` で現在の
  `_adb-tls-connect` のポートを調べるのが確実。

## 失敗した方法・試行ログ（同じ穴に落ちないこと）
- **commit前に必ず構文チェック**(2026-07-25に実害)。`window.__U` の閉じ `};` を `});` にしてしまい、
  モジュール全体が構文エラー＝**アプリが完全に無起動**（画面真っ黒・エラー表示もなし）のままコミットされた。
  1コマンドで検出できる:
  `node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');fs.writeFileSync(process.env.TEMP+'/chk.mjs',h.match(/<script type=\"module\">([\s\S]*?)<\/script>/)[1]);" && node --check "$TEMP/chk.mjs"`
- **HYGデータの入手先が変わった**(2026-07-25)。旧URLは全部404。現行は
  `https://raw.githubusercontent.com/astronexus/HYG-Database/main/hyg/CURRENT/hygdata_v41.csv`。
- **HYGの vx,vy,vz をそのまま使うと架空の動きが混入する**。距離不明星はHYG側が10万pcに置いているため
  見かけの速度が光速の12%等になる。アプリ側の架空距離(2000pc)の星は速度0にすること。
- **検証の期待値を概算のまま計画書に書かない**(2026-07-25)。機能13で「太陽黄経280.0」と概算を書いたため、
  正しい実装(280.38)を検収係が不合格と誤判定した。天文値は手計算で桁まで詰めてから計画書に書く。
- **`<script type="module">` なので `PLANETS`/`mode` 等はコンソールから見えない**(見えなくて正常)。
  検収係が「実行環境に存在しない＝壊れている」と誤読した。状態確認は必ず `window.__U` フック経由。
- 軌道計算で最初に間違えたのは**回転の順序と ω**(2026-07-25)。ω は Z軸まわり、しかも表の5列目は
  ϖ(近日点黄経)なので **ω = ϖ − Ω**。正: ω(Z) → I(X) → Ω(Z)。太陽黄経の検算1回で一発で見抜ける。
- **歴史モードの惑星スプライトは作成時(0,0,0)=地球と同位置**。histFreezeを入った直後に有効化すると
  位置計算が一度も走らず巨大な光が画面を覆い「背景が真っ白」になる→ 作成直後に updatePlanets(0) を
  呼ぶ恒久修正済み(2026-07-08)。全面白＝「原点に取り残された発光スプライト」をまず疑う。
- **tourのフリックはドラッグ回転も兼ねる**(pointermoveで回した後にpointerupで切替判定)ため、
  1フリックでカメラが約200°回り、到着時に夜側(真っ暗)を向く事故→ 飛行中に定番構図(tTh=2.4,tPh=1.35)へ
  イージングで戻す方式で解決。到着直後が暗い時はこれを疑う。
- **プレビューサーバー(uchu-app)はセッション中に何度も勝手に落ちる**。スマホがERR_EMPTY_RESPONSEなら
  まず preview_list で生死確認→再起動。serve.ps1のKeepAlive問題(修正済み)と混同しないこと。
- 実機のボタン座標(1080x2520): 🪐惑星ボタン≈(953,1652)。フリック=input swipe 250 1100 850 1100 120。
- **UI消灯後の見えないボタンがタップを吸う事故**→ .fade に pointer-events:none で修正済み(2026-07-11)。
  副作用: adbでボタンを押すときは「起こすタップ→0.8秒→本タップ」を1コマンド内で行うこと(3秒で再消灯)。
- **カードに pointer-events:auto を付けると、カード上から始めたフリックが吸われる**→ クリック待ちでなく
  onpointerdown で即閉じる方式で解決。UI要素を触れるようにする時はジェスチャーの通り道を考えること。
- **新しいカメラを足したら resize ハンドラへの aspect 更新を忘れずに**(忘れると横画面で球がつぶれる。
  tCamで実際に起きた)。カメラ追加時のチェックリスト: aspect更新+fov同期+モード分岐+HUD。
- **このPCのデスクトップ検証パネルは rAF(描画ループ)が完全停止する**ことがある→ 実時間検証は不可。
  `__U.tourTick(dt)` のような「1コマ手動で進める」フックを作って決定論で検証するのが正解だった。
- 無線adbが切れたら: スマホの「ワイヤレスデバッグ」画面のIP:ポートへ `adb connect IP:PORT`(ペア設定済みなら
  コード不要・ポートは毎回変わる)。2026-07-11は 192.168.31.189:40975 で接続。
- tex/ の実写マップはthree.js r160 examples由来(NASA系パブリックドメイン)。オフライン同梱OK。
- **カメラ用の変数名 `pos`/`vel` は星座標のFloat32Array `pos` と衝突**→モジュール全体が構文エラーで無起動
  （エラー表示なし・`__uchuStarted`もfalseのまま）。カメラ系は `cpos`/`cvel` を使うこと。
  原因調査は `node -e` でscriptタグ中身を `new Function()` に通す構文チェックが最速だった。
- **画面右方向は `cross(viewTarget, camUp)` の順**。逆順(camUp, viewTarget)だと左右が鏡写しになる(実証済み)。
- 遊泳カメラの失敗設計3代: ①方位角+lookAt=極で縦移動制限 ②剛体フレーム大円回転=回るだけで奥行きなし
  ③中心方向ダイブ=中心注視のままだと「後退・旋回」に見える。→ 最終法則(進行方向を見る+明示回頭)で解決。
- 視線を「最短経路で中心へlerp」すると回頭方向がフリックと無関係になり「毎回同じ方向に回る」と感じられる
  → steerAxis(フリック方向で決まる軸)周りの明示回転にすること。
- **serve.ps1 は KeepAlive のままだと adb reverse 経由で ERR_EMPTY_RESPONSE**(スマホだけ読めない)。
  接続ごとに閉じる+エラー時Abort に修正済み(2026-07-05)。スマホが古い版のままの事故はまずこれを疑う。
- adbデーモン再起動直後は `device not found` になる→数秒待って再試行で復帰する(USB再挿入は不要だった)。
- スマホのスクショは撮影前に `dumpsys window | grep mCurrentFocus` でChromeが前面か確認する
  (通知でLINE等が前面になっていると他人の画面を撮ってしまう事故が実際に起きた)。
- **BHの質量を成長させる設計は2度失敗**（全星呑み込み崩壊 or 完全凍結の双安定）。質量900固定+「満員時は最古の星が決定論的な死の螺旋で還る」設計で90分安定を確認済み。変更するなら `__U.advanceSim(5400)` 長期テスト必須。
- ~~このPCは `adb pair` が protocol fault で全滅~~ → **2026-07-25、`adb pair IP:ペア用ポート コード` は普通に成功した**
  （スマホの「ペア設定コードによるペア設定」画面を開いて実行）。`connect` だけ何度やっても失敗する時は
  ペアが切れているだけなので、まず `adb pair` をやり直すこと。USB必須ではなくなった。
- **PowerShellの `>` でスクリーンショットを保存すると壊れる**（BOM+文字コード変換でPNGでなくなる）。
  `adb exec-out screencap -p > file.png` は**Bashツール**で実行すること(2026-07-25)。
- プレビュータブが非表示だと rAF が1〜2fpsに制限される → 実時間待ちの検証は無効。フックで状態を進める。
- スクリーンショットは3〜10秒遅れる → 歴史モード終端(p>0.95)などは撮影前に一周してしまう。余裕のある時点にジャンプして撮る。
- 星が暗すぎ問題は「等級→輝度カーブ」調整で解決済み（vInt式）。安易に露出を上げず既存カーブを尊重。
